parquet_path: ${oc.env:DATA_ROOT,/share/ju/matt/sensing-ai-risks/data}/global_subset/all_articles.parquet
columns:
  # Canonical UAIR column expected by stages is now article_text directly
  article_text: article_text


