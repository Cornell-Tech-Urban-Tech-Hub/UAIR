defaults:
  - override hydra/launcher: submitit_slurm

hydra:
  launcher:
    timeout_min: 1440
    partition: pierson
    gpus_per_node: 2
    cpus_per_task: 6
    mem_gb: 64
    tasks_per_node: 1
    nodes: 1
    name: ${hydra.job.name}
    additional_parameters: {}


