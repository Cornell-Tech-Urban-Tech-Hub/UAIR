hydra:
  run:
    dir: /share/pierson/matt/UAIR/outputs/${now:%Y-%m-%d_%H-%M-%S}
  sweep:
    dir: /share/pierson/matt/UAIR/multirun/${now:%Y-%m-%d_%H-%M-%S}
    subdir: ${hydra.job.override_dirname}
  job:
    config:
      override_dirname:
        exclude_keys: [seed, wandb.mode, wandb.project, run_id, output_dir]


